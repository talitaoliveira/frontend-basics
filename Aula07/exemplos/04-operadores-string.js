// Operadores String

var nome = 'Talita';
var sobremome = 'Oliveira';

// Concatenação
var nomeCompleto = nome + ' ' + sobremome;

console.log(nomeCompleto);


// Outra forma de concatenar string
// uso de crase, e coloca o texto dentro. Se houver variavel para concatenar, usar ${nomeVariavel}
var textoApresentacao = `Olá! Meu nome é ${nome} ${sobremome}! `;

console.log(textoApresentacao);

